<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="post">
	<insert id="insert" parameterType="postvo">
		<![CDATA[
			insert into post values (#{id}, #{title}, #{contents}, now(), #{categoryId})
		]]>
	</insert>
	
	<select id="findDefaultId" parameterType="long" resultType="long">
		<![CDATA[
			select ifnull(max(id), 0) 
			  from post 
			 where category_id=#{categoryId}
		]]>
	</select>
	
	<select id="findById" parameterType="long" resultType="postvo">
		<![CDATA[
			select id,
				   title,
				   contents,
				   date_format(reg_date, '%Y-%m-%d') as regDate,
				   category_id as categoryId
		      from post
		     where id=#{id}
		]]>
	</select>
	
	<select id="findByCategoryId" parameterType="long" resultType="postvo">
		<![CDATA[
			select id,
				   title,
				   contents,
				   date_format(reg_date, '%Y-%m-%d') as regDate,
				   category_id as categoryId
		      from post
		     where category_id=#{categoryId}
		  order by reg_date desc
		]]>
	</select>
	
	<select id="findCategoryId" parameterType="long" resultType="long">
		<![CDATA[
			select category_id as categoryId
		      from post
		     where id=#{postId}
		]]>
	</select>
	
	<delete id = "deleteByCategoryId" parameterType="long">
		<![CDATA[
			delete from post
			 where category_id=#{categoryId}
	  	]]>
	</delete>
</mapper>